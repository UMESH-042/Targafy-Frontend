import 'package:flutter/material.dart';

const Color kprimaryColor = Color(0xFF776B5D);
const Color ksecondaryColor = Color(0xFFB0A695);
const Color ktertiaryColor = Color(0xFFEBE3D5);
const Color kquaternaryColor = Color(0xFFF3EEEA);

const Color successColor = Color(0xFF008000);
const Color notificationColor = Color(0xFFFF5722);
const Color invalidColor = Colors.amber;
const Color failureColor = Colors.amber;
const Color submitButtonsColor = Colors.blue;
const Color errorColor = Colors.amber;

const Color bodyColor = Color(0xffF3EEEA);

const Color textColor = Colors.black;
const Color dividerColor = Colors.black;
const fontFamily = "Poppins";

// Colors.green.shade600 good color

/* errorColor
alertColor
failureColor
warningColor
problemColor
invalidColor
errorHighlightColor
negativeFeedbackColor */
//tile color

Color reassignColor = Colors.blue.withOpacity(0.2);
Color changeDateColor = Colors.purple.withOpacity(0.2);
Color criticalColor = Colors.red.withOpacity(0.2);
Color blockedColor = Colors.black.withOpacity(0.5);
Color closedColor = Colors.green.withOpacity(0.2);
Color changeTitleColor = Colors.yellow.withOpacity(0.2);
// Colors.green.shade600 good color

/* errorColor
alertColor
failureColor
warningColor
problemColor
invalidColor
errorHighlightColor
negativeFeedbackColor */